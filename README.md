# Vue 3 xx Vite xx Three.js

- `npm install`
- `npm i three`
- `npm run dev` | `npm run dev -- --host`
- `npm run build && surge dist/`

# Choosing template

In `App.vue`, comment or uncomment the different canvas.
